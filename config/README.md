# Config

Runtime configuration templates. The primary file is `nomad.env.example` —
copy it to `nomad.env` and fill in your values. `nomad.env` is gitignored.

- `nomad.env.example`: Template for all runtime configuration
- `nomad.env`: Your local config (gitignored, never commit secrets)
